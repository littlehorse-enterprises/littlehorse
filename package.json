{
  "devDependencies": {
    "prettier": "^2.8.7",
    "prettier-plugin-java": "1.6.1",
    "semver": "^7.5.1"
  },
  "scripts": {
    "format": "prettier --write \"{,**/}*.java\"",
    "check-format": "prettier --check \"{,**/}*.java\"",
    "lint": "npm run check-format",
    "semver": "semver"
  }
}
