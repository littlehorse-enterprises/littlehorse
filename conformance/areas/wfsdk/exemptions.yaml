# Surface capabilities excused from needing a case; freshness ratchets both
# ways, so a stale entry fails. todo = awaiting a case; not_applicable = no
# compiled-output effect (with the reason).
todo: []
not_applicable:
  - id: ExternalEventNodeOutput#registeredAs
    reason: shapes the PutExternalEventDefRequest side-registration, not the WfSpec; proven by the registerWfSpec matrix test against the fake server
  - id: ExternalEventNodeOutput#withCorrelatedEventConfig
    reason: shapes the PutExternalEventDefRequest side-registration, not the WfSpec; proven by its matrix test
  - id: IfElseBody#body
    reason: the SAM of a callback type; user code implements it, nothing compiles from the method itself
  - id: InterruptHandler#withEventType
    reason: shapes the ExternalEventDef side-registration, not the WfSpec — proven vacuous by the differ guard, same class as registeredAs
  - id: SpawnedThreads#buildNode
    reason: compiler plumbing invoked internally by waitForThreads; its output is proven by the wait-for-threads probe
  - id: ThreadFunc#threadFunction
    reason: the SAM of a callback type; user code implements it, nothing compiles from the method itself
  - id: ThrowEventNodeOutput#registeredAs
    reason: shapes the PutWorkflowEventDefRequest side-registration, not the WfSpec; proven by the registerWfSpec matrix test against the fake server
  - id: Workflow#compileAndSaveToDisk
    reason: file-IO wrapper over compileWfToJson; proven by its matrix test
  - id: Workflow#compileWfToJson
    reason: serialization wrapper over compileWorkflow (which the workflow-minimal probe pins); proven by its matrix test
  - id: Workflow#doesWfSpecExist
    reason: an RPC, not a compilation; proven against the fake server
  - id: Workflow#getDefaultTaskTimeout
    reason: query over the builder; the setter that HAS compiled effect is backlogged
  - id: Workflow#getExternalEventDefsToRegister
    reason: query over side-registrations; proven by its own matrix test
  - id: Workflow#getName
    reason: query over the builder; no compiled effect
  - id: Workflow#getPlaceholderValues
    reason: query over the builder; placeholder RESOLUTION is a visible matrix todo
  - id: Workflow#getRequiredChildWfSpecNames
    reason: query derived from compilation; proven by its own matrix test
  - id: Workflow#getRequiredExternalEventDefNames
    reason: query derived from compilation; proven by its own matrix test
  - id: Workflow#getRequiredTaskDefNames
    reason: query derived from compilation; proven by its own matrix test
  - id: Workflow#getRequiredWorkflowEventDefNames
    reason: query derived from compilation; proven by its own matrix test
  - id: Workflow#getTypeAdapterRegistry
    reason: query over the config wiring; adapters affect values at runtime, not the compiled spec
  - id: Workflow#getWorkflowEventDefsToRegister
    reason: query over side-registrations; proven by its own matrix test
  - id: Workflow#registerWfSpec
    reason: an RPC, not a compilation; proven against the fake server (event defs first, then the spec)
