# workflow-minimal

Build `probe-workflow-minimal` (R10) exercising `Workflow#newWorkflow`, `Workflow#compileWorkflow`.
Single-variant: the capability is the precondition of any output, so no
base exists.
The reference body is in WfsdkArea.java (see rules.md, "Minting"); the
frozen fixtures are the contract. Rules: [../../rules.md](../../rules.md).
