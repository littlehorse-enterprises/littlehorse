# Arms excused from needing a case (rules.md S3); freshness ratchets both
# ways, so a stale entry fails.
todo:
  - id: wfRunId
    reason: input needs the WfRunId string convention; typed-input design pending
  - id: struct
    reason: struct conversion needs a StructDef context; case design pending
  - id: array
    reason: native typed arrays need a declared element type; typed-input design pending
  - id: map
    reason: native typed maps need declared key/value types; typed-input design pending
not_applicable: []
