# reg-workflow-event

Build `probe-reg-workflow-event`: throwEvent("milestone", payload) with the feature adding .registeredAs(String payload). The answer is the registrations
document (rules.md G1), not the WfSpec. Delta per rules.md.
