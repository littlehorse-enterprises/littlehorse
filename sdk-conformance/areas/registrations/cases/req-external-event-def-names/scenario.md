# req-external-event-def-names

Build `probe-req-external-event-def-names`: the feature adds waitForEvent("payment-received") without registering it. The answer is the registrations
document (rules.md G1), not the WfSpec. Delta per rules.md.
