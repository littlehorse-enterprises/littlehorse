# req-task-def-names

Build `probe-req-task-def-names`: the feature adds execute("noop"), so "noop" appears in requiredTaskDefNames. The answer is the registrations
document (rules.md G1), not the WfSpec. Delta per rules.md.
