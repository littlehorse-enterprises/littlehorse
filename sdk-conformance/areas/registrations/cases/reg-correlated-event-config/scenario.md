# reg-correlated-event-config

Build `probe-reg-correlated-event-config`: a registered "payment-received" event, with the feature adding .withCorrelatedEventConfig(ttl 3600s, delete after first correlation). The answer is the registrations
document (rules.md G1), not the WfSpec. Delta per rules.md.
