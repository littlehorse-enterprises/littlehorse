# reg-interrupt-event-type

Build `probe-reg-interrupt-event-type`: an interrupt handler on "cancel-requested", with the feature adding .withEventType(String payload). The answer is the registrations
document (rules.md G1), not the WfSpec. Delta per rules.md.
