# req-workflow-event-def-names

Build `probe-req-workflow-event-def-names`: the feature adds throwEvent("milestone", payload) without registering it. The answer is the registrations
document (rules.md G1), not the WfSpec. Delta per rules.md.
