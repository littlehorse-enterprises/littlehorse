# reg-external-event

Build `probe-reg-external-event`: waitForEvent("payment-received") with the feature adding .registeredAs(String payload). The answer is the registrations
document (rules.md G1), not the WfSpec. Delta per rules.md.
