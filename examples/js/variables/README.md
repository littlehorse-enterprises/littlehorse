# JavaScript multi-variable pipeline example

Like [`examples/java/variables`](../../../java/variables/README.md) without WfSpec `searchable` / `masked` / JSON indexes (not in the JS builder yet).

## Run

```bash
npm install
npm start
```

```bash
npm run run-wf
npm run run-wf -- "custom text" false 99
```
