// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: type_definition.proto
// Protobuf Java Version: 4.32.1

package io.littlehorse.sdk.common.proto;

@com.google.protobuf.Generated
public interface MapOrBuilder extends
    // @@protoc_insertion_point(interface_extends:littlehorse.Map)
    com.google.protobuf.MessageOrBuilder {

  /**
   * <pre>
   * The entries of the map.
   *
   * Note: We use a repeated list of entries rather than a protobuf `map&lt;...&gt;` because
   * protobuf only permits scalar (string/integer/bool) keys, whereas a LittleHorse
   * `Map` key is a full `VariableValue` (which may be a `WF_RUN_ID`, `TIMESTAMP`, etc.).
   * </pre>
   *
   * <code>repeated .littlehorse.Map.Entry entries = 1;</code>
   */
  java.util.List<io.littlehorse.sdk.common.proto.Map.Entry> 
      getEntriesList();
  /**
   * <pre>
   * The entries of the map.
   *
   * Note: We use a repeated list of entries rather than a protobuf `map&lt;...&gt;` because
   * protobuf only permits scalar (string/integer/bool) keys, whereas a LittleHorse
   * `Map` key is a full `VariableValue` (which may be a `WF_RUN_ID`, `TIMESTAMP`, etc.).
   * </pre>
   *
   * <code>repeated .littlehorse.Map.Entry entries = 1;</code>
   */
  io.littlehorse.sdk.common.proto.Map.Entry getEntries(int index);
  /**
   * <pre>
   * The entries of the map.
   *
   * Note: We use a repeated list of entries rather than a protobuf `map&lt;...&gt;` because
   * protobuf only permits scalar (string/integer/bool) keys, whereas a LittleHorse
   * `Map` key is a full `VariableValue` (which may be a `WF_RUN_ID`, `TIMESTAMP`, etc.).
   * </pre>
   *
   * <code>repeated .littlehorse.Map.Entry entries = 1;</code>
   */
  int getEntriesCount();
  /**
   * <pre>
   * The entries of the map.
   *
   * Note: We use a repeated list of entries rather than a protobuf `map&lt;...&gt;` because
   * protobuf only permits scalar (string/integer/bool) keys, whereas a LittleHorse
   * `Map` key is a full `VariableValue` (which may be a `WF_RUN_ID`, `TIMESTAMP`, etc.).
   * </pre>
   *
   * <code>repeated .littlehorse.Map.Entry entries = 1;</code>
   */
  java.util.List<? extends io.littlehorse.sdk.common.proto.Map.EntryOrBuilder> 
      getEntriesOrBuilderList();
  /**
   * <pre>
   * The entries of the map.
   *
   * Note: We use a repeated list of entries rather than a protobuf `map&lt;...&gt;` because
   * protobuf only permits scalar (string/integer/bool) keys, whereas a LittleHorse
   * `Map` key is a full `VariableValue` (which may be a `WF_RUN_ID`, `TIMESTAMP`, etc.).
   * </pre>
   *
   * <code>repeated .littlehorse.Map.Entry entries = 1;</code>
   */
  io.littlehorse.sdk.common.proto.Map.EntryOrBuilder getEntriesOrBuilder(
      int index);

  /**
   * <pre>
   * Authoritative key/value types for this map.
   * Stored alongside the entries for ease of access, mirroring `Array.element_type`.
   *
   * Native Maps are always typed: this field is `optional` only for wire-compatibility with
   * Maps persisted before typing was enforced. When absent (legacy data), the server infers the
   * type from the entries on read; empty legacy Maps are typed on assignment. New Maps must
   * always carry a concrete key/value type.
   * </pre>
   *
   * <code>optional .littlehorse.InlineMapDef map_type = 2;</code>
   * @return Whether the mapType field is set.
   */
  boolean hasMapType();
  /**
   * <pre>
   * Authoritative key/value types for this map.
   * Stored alongside the entries for ease of access, mirroring `Array.element_type`.
   *
   * Native Maps are always typed: this field is `optional` only for wire-compatibility with
   * Maps persisted before typing was enforced. When absent (legacy data), the server infers the
   * type from the entries on read; empty legacy Maps are typed on assignment. New Maps must
   * always carry a concrete key/value type.
   * </pre>
   *
   * <code>optional .littlehorse.InlineMapDef map_type = 2;</code>
   * @return The mapType.
   */
  io.littlehorse.sdk.common.proto.InlineMapDef getMapType();
  /**
   * <pre>
   * Authoritative key/value types for this map.
   * Stored alongside the entries for ease of access, mirroring `Array.element_type`.
   *
   * Native Maps are always typed: this field is `optional` only for wire-compatibility with
   * Maps persisted before typing was enforced. When absent (legacy data), the server infers the
   * type from the entries on read; empty legacy Maps are typed on assignment. New Maps must
   * always carry a concrete key/value type.
   * </pre>
   *
   * <code>optional .littlehorse.InlineMapDef map_type = 2;</code>
   */
  io.littlehorse.sdk.common.proto.InlineMapDefOrBuilder getMapTypeOrBuilder();
}
