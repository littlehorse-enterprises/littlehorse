"use client"
import { ReactFlowProvider } from "@xyflow/react"
import { Layouter } from "../(authenticated)/[tenantId]/(diagram)/components/NewLayouter"

export default function TestPage() {
    return <Layouter />
}
