// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.32.1
// source: workflow_migration.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { VariableAssignment } from "./common_wfspec";
import { Timestamp } from "./google/protobuf/timestamp";
import { WfSpecId, WorkflowMigrationPlanId } from "./object_id";

export interface WorkflowMigrationPlan {
  /** The id of the migration plann */
  workflowMigrationPlanId:
    | WorkflowMigrationPlanId
    | undefined;
  /** Time WorkflowMigrationPlan was created */
  createdAt:
    | string
    | undefined;
  /** Map that represent old threadSpec name -> How to migrate that threadspec */
  threadMigrations: { [key: string]: ThreadMigrationPlan };
  /** Source wfSpec */
  oldWfSpec:
    | WfSpecId
    | undefined;
  /** major_version and revision of the destination specs */
  majorVersion: number;
  revision: number;
}

export interface WorkflowMigrationPlan_ThreadMigrationsEntry {
  key: string;
  value: ThreadMigrationPlan | undefined;
}

export interface ThreadMigrationPlan {
  /**
   * Name of thread in new wfSpec that thread wants
   * to migrate to
   */
  newThreadName: string;
  /** Name of node in old thread to migrate from */
  fromNode: string;
  /** name of node to migrate to within new spec */
  toNode: string;
  /**
   * list of required variables that muse either be
   * updated or inserted at runtime when migration is called
   */
  requiredVariables: string[];
  dependencies: string[];
}

export interface MigrationVars {
  varAssignmentByVarName: { [key: string]: VariableAssignment };
}

export interface MigrationVars_VarAssignmentByVarNameEntry {
  key: string;
  value: VariableAssignment | undefined;
}

function createBaseWorkflowMigrationPlan(): WorkflowMigrationPlan {
  return {
    workflowMigrationPlanId: undefined,
    createdAt: undefined,
    threadMigrations: {},
    oldWfSpec: undefined,
    majorVersion: 0,
    revision: 0,
  };
}

export const WorkflowMigrationPlan = {
  encode(message: WorkflowMigrationPlan, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.workflowMigrationPlanId !== undefined) {
      WorkflowMigrationPlanId.encode(message.workflowMigrationPlanId, writer.uint32(10).fork()).ldelim();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).ldelim();
    }
    Object.entries(message.threadMigrations).forEach(([key, value]) => {
      WorkflowMigrationPlan_ThreadMigrationsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).ldelim();
    });
    if (message.oldWfSpec !== undefined) {
      WfSpecId.encode(message.oldWfSpec, writer.uint32(34).fork()).ldelim();
    }
    if (message.majorVersion !== 0) {
      writer.uint32(40).int32(message.majorVersion);
    }
    if (message.revision !== 0) {
      writer.uint32(48).int32(message.revision);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): WorkflowMigrationPlan {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowMigrationPlan();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.workflowMigrationPlanId = WorkflowMigrationPlanId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          const entry3 = WorkflowMigrationPlan_ThreadMigrationsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.threadMigrations[entry3.key] = entry3.value;
          }
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.oldWfSpec = WfSpecId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.majorVersion = reader.int32();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.revision = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowMigrationPlan {
    return {
      workflowMigrationPlanId: isSet(object.workflowMigrationPlanId)
        ? WorkflowMigrationPlanId.fromJSON(object.workflowMigrationPlanId)
        : undefined,
      createdAt: isSet(object.createdAt) ? globalThis.String(object.createdAt) : undefined,
      threadMigrations: isObject(object.threadMigrations)
        ? Object.entries(object.threadMigrations).reduce<{ [key: string]: ThreadMigrationPlan }>(
          (acc, [key, value]) => {
            acc[key] = ThreadMigrationPlan.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      oldWfSpec: isSet(object.oldWfSpec) ? WfSpecId.fromJSON(object.oldWfSpec) : undefined,
      majorVersion: isSet(object.majorVersion) ? globalThis.Number(object.majorVersion) : 0,
      revision: isSet(object.revision) ? globalThis.Number(object.revision) : 0,
    };
  },

  toJSON(message: WorkflowMigrationPlan): unknown {
    const obj: any = {};
    if (message.workflowMigrationPlanId !== undefined) {
      obj.workflowMigrationPlanId = WorkflowMigrationPlanId.toJSON(message.workflowMigrationPlanId);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt;
    }
    if (message.threadMigrations) {
      const entries = Object.entries(message.threadMigrations);
      if (entries.length > 0) {
        obj.threadMigrations = {};
        entries.forEach(([k, v]) => {
          obj.threadMigrations[k] = ThreadMigrationPlan.toJSON(v);
        });
      }
    }
    if (message.oldWfSpec !== undefined) {
      obj.oldWfSpec = WfSpecId.toJSON(message.oldWfSpec);
    }
    if (message.majorVersion !== 0) {
      obj.majorVersion = Math.round(message.majorVersion);
    }
    if (message.revision !== 0) {
      obj.revision = Math.round(message.revision);
    }
    return obj;
  },

  create(base?: DeepPartial<WorkflowMigrationPlan>): WorkflowMigrationPlan {
    return WorkflowMigrationPlan.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WorkflowMigrationPlan>): WorkflowMigrationPlan {
    const message = createBaseWorkflowMigrationPlan();
    message.workflowMigrationPlanId =
      (object.workflowMigrationPlanId !== undefined && object.workflowMigrationPlanId !== null)
        ? WorkflowMigrationPlanId.fromPartial(object.workflowMigrationPlanId)
        : undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.threadMigrations = Object.entries(object.threadMigrations ?? {}).reduce<
      { [key: string]: ThreadMigrationPlan }
    >((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = ThreadMigrationPlan.fromPartial(value);
      }
      return acc;
    }, {});
    message.oldWfSpec = (object.oldWfSpec !== undefined && object.oldWfSpec !== null)
      ? WfSpecId.fromPartial(object.oldWfSpec)
      : undefined;
    message.majorVersion = object.majorVersion ?? 0;
    message.revision = object.revision ?? 0;
    return message;
  },
};

function createBaseWorkflowMigrationPlan_ThreadMigrationsEntry(): WorkflowMigrationPlan_ThreadMigrationsEntry {
  return { key: "", value: undefined };
}

export const WorkflowMigrationPlan_ThreadMigrationsEntry = {
  encode(message: WorkflowMigrationPlan_ThreadMigrationsEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      ThreadMigrationPlan.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): WorkflowMigrationPlan_ThreadMigrationsEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowMigrationPlan_ThreadMigrationsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = ThreadMigrationPlan.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowMigrationPlan_ThreadMigrationsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? ThreadMigrationPlan.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: WorkflowMigrationPlan_ThreadMigrationsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = ThreadMigrationPlan.toJSON(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<WorkflowMigrationPlan_ThreadMigrationsEntry>): WorkflowMigrationPlan_ThreadMigrationsEntry {
    return WorkflowMigrationPlan_ThreadMigrationsEntry.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<WorkflowMigrationPlan_ThreadMigrationsEntry>,
  ): WorkflowMigrationPlan_ThreadMigrationsEntry {
    const message = createBaseWorkflowMigrationPlan_ThreadMigrationsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? ThreadMigrationPlan.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseThreadMigrationPlan(): ThreadMigrationPlan {
  return { newThreadName: "", fromNode: "", toNode: "", requiredVariables: [], dependencies: [] };
}

export const ThreadMigrationPlan = {
  encode(message: ThreadMigrationPlan, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.newThreadName !== "") {
      writer.uint32(10).string(message.newThreadName);
    }
    if (message.fromNode !== "") {
      writer.uint32(18).string(message.fromNode);
    }
    if (message.toNode !== "") {
      writer.uint32(26).string(message.toNode);
    }
    for (const v of message.requiredVariables) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.dependencies) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ThreadMigrationPlan {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThreadMigrationPlan();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.newThreadName = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.fromNode = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.toNode = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.requiredVariables.push(reader.string());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.dependencies.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThreadMigrationPlan {
    return {
      newThreadName: isSet(object.newThreadName) ? globalThis.String(object.newThreadName) : "",
      fromNode: isSet(object.fromNode) ? globalThis.String(object.fromNode) : "",
      toNode: isSet(object.toNode) ? globalThis.String(object.toNode) : "",
      requiredVariables: globalThis.Array.isArray(object?.requiredVariables)
        ? object.requiredVariables.map((e: any) => globalThis.String(e))
        : [],
      dependencies: globalThis.Array.isArray(object?.dependencies)
        ? object.dependencies.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ThreadMigrationPlan): unknown {
    const obj: any = {};
    if (message.newThreadName !== "") {
      obj.newThreadName = message.newThreadName;
    }
    if (message.fromNode !== "") {
      obj.fromNode = message.fromNode;
    }
    if (message.toNode !== "") {
      obj.toNode = message.toNode;
    }
    if (message.requiredVariables?.length) {
      obj.requiredVariables = message.requiredVariables;
    }
    if (message.dependencies?.length) {
      obj.dependencies = message.dependencies;
    }
    return obj;
  },

  create(base?: DeepPartial<ThreadMigrationPlan>): ThreadMigrationPlan {
    return ThreadMigrationPlan.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ThreadMigrationPlan>): ThreadMigrationPlan {
    const message = createBaseThreadMigrationPlan();
    message.newThreadName = object.newThreadName ?? "";
    message.fromNode = object.fromNode ?? "";
    message.toNode = object.toNode ?? "";
    message.requiredVariables = object.requiredVariables?.map((e) => e) || [];
    message.dependencies = object.dependencies?.map((e) => e) || [];
    return message;
  },
};

function createBaseMigrationVars(): MigrationVars {
  return { varAssignmentByVarName: {} };
}

export const MigrationVars = {
  encode(message: MigrationVars, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    Object.entries(message.varAssignmentByVarName).forEach(([key, value]) => {
      MigrationVars_VarAssignmentByVarNameEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).ldelim();
    });
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MigrationVars {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMigrationVars();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          const entry1 = MigrationVars_VarAssignmentByVarNameEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.varAssignmentByVarName[entry1.key] = entry1.value;
          }
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MigrationVars {
    return {
      varAssignmentByVarName: isObject(object.varAssignmentByVarName)
        ? Object.entries(object.varAssignmentByVarName).reduce<{ [key: string]: VariableAssignment }>(
          (acc, [key, value]) => {
            acc[key] = VariableAssignment.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: MigrationVars): unknown {
    const obj: any = {};
    if (message.varAssignmentByVarName) {
      const entries = Object.entries(message.varAssignmentByVarName);
      if (entries.length > 0) {
        obj.varAssignmentByVarName = {};
        entries.forEach(([k, v]) => {
          obj.varAssignmentByVarName[k] = VariableAssignment.toJSON(v);
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<MigrationVars>): MigrationVars {
    return MigrationVars.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MigrationVars>): MigrationVars {
    const message = createBaseMigrationVars();
    message.varAssignmentByVarName = Object.entries(object.varAssignmentByVarName ?? {}).reduce<
      { [key: string]: VariableAssignment }
    >((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = VariableAssignment.fromPartial(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBaseMigrationVars_VarAssignmentByVarNameEntry(): MigrationVars_VarAssignmentByVarNameEntry {
  return { key: "", value: undefined };
}

export const MigrationVars_VarAssignmentByVarNameEntry = {
  encode(message: MigrationVars_VarAssignmentByVarNameEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      VariableAssignment.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MigrationVars_VarAssignmentByVarNameEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMigrationVars_VarAssignmentByVarNameEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = VariableAssignment.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MigrationVars_VarAssignmentByVarNameEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? VariableAssignment.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: MigrationVars_VarAssignmentByVarNameEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = VariableAssignment.toJSON(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<MigrationVars_VarAssignmentByVarNameEntry>): MigrationVars_VarAssignmentByVarNameEntry {
    return MigrationVars_VarAssignmentByVarNameEntry.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<MigrationVars_VarAssignmentByVarNameEntry>,
  ): MigrationVars_VarAssignmentByVarNameEntry {
    const message = createBaseMigrationVars_VarAssignmentByVarNameEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? VariableAssignment.fromPartial(object.value)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
