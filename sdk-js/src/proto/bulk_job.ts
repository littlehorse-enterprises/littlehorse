// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.32.1
// source: bulk_job.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { LHStatus, lHStatusFromJSON, lHStatusToJSON, lHStatusToNumber } from "./common_enums";
import { Timestamp } from "./google/protobuf/timestamp";
import { BulkJobId } from "./object_id";

/** The status of a BulkJob or BulkJobShard. */
export enum BulkJobStatus {
  /** BULK_JOB_RUNNING - The job is actively being processed. This is the initial state. */
  BULK_JOB_RUNNING = "BULK_JOB_RUNNING",
  /** BULK_JOB_COMPLETED - The job completed successfully. */
  BULK_JOB_COMPLETED = "BULK_JOB_COMPLETED",
  /** BULK_JOB_FAILED - The job failed. */
  BULK_JOB_FAILED = "BULK_JOB_FAILED",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function bulkJobStatusFromJSON(object: any): BulkJobStatus {
  switch (object) {
    case 0:
    case "BULK_JOB_RUNNING":
      return BulkJobStatus.BULK_JOB_RUNNING;
    case 1:
    case "BULK_JOB_COMPLETED":
      return BulkJobStatus.BULK_JOB_COMPLETED;
    case 2:
    case "BULK_JOB_FAILED":
      return BulkJobStatus.BULK_JOB_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return BulkJobStatus.UNRECOGNIZED;
  }
}

export function bulkJobStatusToJSON(object: BulkJobStatus): string {
  switch (object) {
    case BulkJobStatus.BULK_JOB_RUNNING:
      return "BULK_JOB_RUNNING";
    case BulkJobStatus.BULK_JOB_COMPLETED:
      return "BULK_JOB_COMPLETED";
    case BulkJobStatus.BULK_JOB_FAILED:
      return "BULK_JOB_FAILED";
    case BulkJobStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function bulkJobStatusToNumber(object: BulkJobStatus): number {
  switch (object) {
    case BulkJobStatus.BULK_JOB_RUNNING:
      return 0;
    case BulkJobStatus.BULK_JOB_COMPLETED:
      return 1;
    case BulkJobStatus.BULK_JOB_FAILED:
      return 2;
    case BulkJobStatus.UNRECOGNIZED:
    default:
      return -1;
  }
}

/** A BulkJob represents a long-running background operation that affects many objects. */
export interface BulkJob {
  /** The ID of this BulkJob. */
  id:
    | BulkJobId
    | undefined;
  /** When the BulkJob was created. */
  createdAt:
    | string
    | undefined;
  /** Current status of the BulkJob. */
  status: BulkJobStatus;
  operation?:
    | { $case: "bulkDeleteWfRun"; value: BulkDeleteWfRun }
    | undefined;
  /** Progress information: total number of items to process. */
  totalItems: number;
  /** Progress information: number of items processed so far. */
  processedItems: number;
}

/**
 * Represents a shard of a BulkJob running on a single partition.
 * This is stored in the per-partition Core Store since each partition independently
 * manages its own shard.
 */
export interface BulkJobShard {
  /** The BulkJob this shard belongs to. */
  bulkJobId:
    | BulkJobId
    | undefined;
  /** The partition this shard is operating on. */
  partition: number;
  /** Current status of this shard. */
  status: BulkJobStatus;
  /** Number of items processed by this shard so far. */
  processedItems: number;
}

/** Describes a bulk deletion of WfRun's matching certain criteria. */
export interface BulkDeleteWfRun {
  /** The name of the WfSpec whose WfRun's should be deleted. Required. */
  wfSpecName: string;
  /** Only delete WfRun's that started at or after this time. Required. */
  earliestStart:
    | string
    | undefined;
  /** Only delete WfRun's that started at or before this time. Required. */
  latestStart:
    | string
    | undefined;
  /** If set, only delete WfRun's with this status. */
  status?: LHStatus | undefined;
}

/** Request to create a BulkJob. */
export interface CreateBulkJobRequest {
  /** Optional client-provided ID for idempotency. */
  id?: string | undefined;
  operation?: { $case: "bulkDeleteWfRun"; value: BulkDeleteWfRun } | undefined;
}

/** Request to get the status of a BulkJob. */
export interface GetBulkJobRequest {
  /** The ID of the BulkJob to retrieve. */
  id: BulkJobId | undefined;
}

function createBaseBulkJob(): BulkJob {
  return {
    id: undefined,
    createdAt: undefined,
    status: BulkJobStatus.BULK_JOB_RUNNING,
    operation: undefined,
    totalItems: 0,
    processedItems: 0,
  };
}

export const BulkJob = {
  encode(message: BulkJob, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      BulkJobId.encode(message.id, writer.uint32(10).fork()).ldelim();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).ldelim();
    }
    if (message.status !== BulkJobStatus.BULK_JOB_RUNNING) {
      writer.uint32(24).int32(bulkJobStatusToNumber(message.status));
    }
    switch (message.operation?.$case) {
      case "bulkDeleteWfRun":
        BulkDeleteWfRun.encode(message.operation.value, writer.uint32(82).fork()).ldelim();
        break;
    }
    if (message.totalItems !== 0) {
      writer.uint32(160).int64(message.totalItems);
    }
    if (message.processedItems !== 0) {
      writer.uint32(168).int64(message.processedItems);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BulkJob {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBulkJob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = BulkJobId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.status = bulkJobStatusFromJSON(reader.int32());
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.operation = { $case: "bulkDeleteWfRun", value: BulkDeleteWfRun.decode(reader, reader.uint32()) };
          continue;
        case 20:
          if (tag !== 160) {
            break;
          }

          message.totalItems = longToNumber(reader.int64() as Long);
          continue;
        case 21:
          if (tag !== 168) {
            break;
          }

          message.processedItems = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BulkJob {
    return {
      id: isSet(object.id) ? BulkJobId.fromJSON(object.id) : undefined,
      createdAt: isSet(object.createdAt) ? globalThis.String(object.createdAt) : undefined,
      status: isSet(object.status) ? bulkJobStatusFromJSON(object.status) : BulkJobStatus.BULK_JOB_RUNNING,
      operation: isSet(object.bulkDeleteWfRun)
        ? { $case: "bulkDeleteWfRun", value: BulkDeleteWfRun.fromJSON(object.bulkDeleteWfRun) }
        : undefined,
      totalItems: isSet(object.totalItems) ? globalThis.Number(object.totalItems) : 0,
      processedItems: isSet(object.processedItems) ? globalThis.Number(object.processedItems) : 0,
    };
  },

  toJSON(message: BulkJob): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = BulkJobId.toJSON(message.id);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt;
    }
    if (message.status !== BulkJobStatus.BULK_JOB_RUNNING) {
      obj.status = bulkJobStatusToJSON(message.status);
    }
    if (message.operation?.$case === "bulkDeleteWfRun") {
      obj.bulkDeleteWfRun = BulkDeleteWfRun.toJSON(message.operation.value);
    }
    if (message.totalItems !== 0) {
      obj.totalItems = Math.round(message.totalItems);
    }
    if (message.processedItems !== 0) {
      obj.processedItems = Math.round(message.processedItems);
    }
    return obj;
  },

  create(base?: DeepPartial<BulkJob>): BulkJob {
    return BulkJob.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BulkJob>): BulkJob {
    const message = createBaseBulkJob();
    message.id = (object.id !== undefined && object.id !== null) ? BulkJobId.fromPartial(object.id) : undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.status = object.status ?? BulkJobStatus.BULK_JOB_RUNNING;
    if (
      object.operation?.$case === "bulkDeleteWfRun" &&
      object.operation?.value !== undefined &&
      object.operation?.value !== null
    ) {
      message.operation = { $case: "bulkDeleteWfRun", value: BulkDeleteWfRun.fromPartial(object.operation.value) };
    }
    message.totalItems = object.totalItems ?? 0;
    message.processedItems = object.processedItems ?? 0;
    return message;
  },
};

function createBaseBulkJobShard(): BulkJobShard {
  return { bulkJobId: undefined, partition: 0, status: BulkJobStatus.BULK_JOB_RUNNING, processedItems: 0 };
}

export const BulkJobShard = {
  encode(message: BulkJobShard, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.bulkJobId !== undefined) {
      BulkJobId.encode(message.bulkJobId, writer.uint32(10).fork()).ldelim();
    }
    if (message.partition !== 0) {
      writer.uint32(16).int32(message.partition);
    }
    if (message.status !== BulkJobStatus.BULK_JOB_RUNNING) {
      writer.uint32(24).int32(bulkJobStatusToNumber(message.status));
    }
    if (message.processedItems !== 0) {
      writer.uint32(32).int64(message.processedItems);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BulkJobShard {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBulkJobShard();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.bulkJobId = BulkJobId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.partition = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.status = bulkJobStatusFromJSON(reader.int32());
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.processedItems = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BulkJobShard {
    return {
      bulkJobId: isSet(object.bulkJobId) ? BulkJobId.fromJSON(object.bulkJobId) : undefined,
      partition: isSet(object.partition) ? globalThis.Number(object.partition) : 0,
      status: isSet(object.status) ? bulkJobStatusFromJSON(object.status) : BulkJobStatus.BULK_JOB_RUNNING,
      processedItems: isSet(object.processedItems) ? globalThis.Number(object.processedItems) : 0,
    };
  },

  toJSON(message: BulkJobShard): unknown {
    const obj: any = {};
    if (message.bulkJobId !== undefined) {
      obj.bulkJobId = BulkJobId.toJSON(message.bulkJobId);
    }
    if (message.partition !== 0) {
      obj.partition = Math.round(message.partition);
    }
    if (message.status !== BulkJobStatus.BULK_JOB_RUNNING) {
      obj.status = bulkJobStatusToJSON(message.status);
    }
    if (message.processedItems !== 0) {
      obj.processedItems = Math.round(message.processedItems);
    }
    return obj;
  },

  create(base?: DeepPartial<BulkJobShard>): BulkJobShard {
    return BulkJobShard.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BulkJobShard>): BulkJobShard {
    const message = createBaseBulkJobShard();
    message.bulkJobId = (object.bulkJobId !== undefined && object.bulkJobId !== null)
      ? BulkJobId.fromPartial(object.bulkJobId)
      : undefined;
    message.partition = object.partition ?? 0;
    message.status = object.status ?? BulkJobStatus.BULK_JOB_RUNNING;
    message.processedItems = object.processedItems ?? 0;
    return message;
  },
};

function createBaseBulkDeleteWfRun(): BulkDeleteWfRun {
  return { wfSpecName: "", earliestStart: undefined, latestStart: undefined, status: undefined };
}

export const BulkDeleteWfRun = {
  encode(message: BulkDeleteWfRun, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.wfSpecName !== "") {
      writer.uint32(10).string(message.wfSpecName);
    }
    if (message.earliestStart !== undefined) {
      Timestamp.encode(toTimestamp(message.earliestStart), writer.uint32(18).fork()).ldelim();
    }
    if (message.latestStart !== undefined) {
      Timestamp.encode(toTimestamp(message.latestStart), writer.uint32(26).fork()).ldelim();
    }
    if (message.status !== undefined) {
      writer.uint32(32).int32(lHStatusToNumber(message.status));
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BulkDeleteWfRun {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBulkDeleteWfRun();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.wfSpecName = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.earliestStart = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.latestStart = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.status = lHStatusFromJSON(reader.int32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BulkDeleteWfRun {
    return {
      wfSpecName: isSet(object.wfSpecName) ? globalThis.String(object.wfSpecName) : "",
      earliestStart: isSet(object.earliestStart) ? globalThis.String(object.earliestStart) : undefined,
      latestStart: isSet(object.latestStart) ? globalThis.String(object.latestStart) : undefined,
      status: isSet(object.status) ? lHStatusFromJSON(object.status) : undefined,
    };
  },

  toJSON(message: BulkDeleteWfRun): unknown {
    const obj: any = {};
    if (message.wfSpecName !== "") {
      obj.wfSpecName = message.wfSpecName;
    }
    if (message.earliestStart !== undefined) {
      obj.earliestStart = message.earliestStart;
    }
    if (message.latestStart !== undefined) {
      obj.latestStart = message.latestStart;
    }
    if (message.status !== undefined) {
      obj.status = lHStatusToJSON(message.status);
    }
    return obj;
  },

  create(base?: DeepPartial<BulkDeleteWfRun>): BulkDeleteWfRun {
    return BulkDeleteWfRun.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BulkDeleteWfRun>): BulkDeleteWfRun {
    const message = createBaseBulkDeleteWfRun();
    message.wfSpecName = object.wfSpecName ?? "";
    message.earliestStart = object.earliestStart ?? undefined;
    message.latestStart = object.latestStart ?? undefined;
    message.status = object.status ?? undefined;
    return message;
  },
};

function createBaseCreateBulkJobRequest(): CreateBulkJobRequest {
  return { id: undefined, operation: undefined };
}

export const CreateBulkJobRequest = {
  encode(message: CreateBulkJobRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    switch (message.operation?.$case) {
      case "bulkDeleteWfRun":
        BulkDeleteWfRun.encode(message.operation.value, writer.uint32(82).fork()).ldelim();
        break;
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateBulkJobRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateBulkJobRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.operation = { $case: "bulkDeleteWfRun", value: BulkDeleteWfRun.decode(reader, reader.uint32()) };
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateBulkJobRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      operation: isSet(object.bulkDeleteWfRun)
        ? { $case: "bulkDeleteWfRun", value: BulkDeleteWfRun.fromJSON(object.bulkDeleteWfRun) }
        : undefined,
    };
  },

  toJSON(message: CreateBulkJobRequest): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.operation?.$case === "bulkDeleteWfRun") {
      obj.bulkDeleteWfRun = BulkDeleteWfRun.toJSON(message.operation.value);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateBulkJobRequest>): CreateBulkJobRequest {
    return CreateBulkJobRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateBulkJobRequest>): CreateBulkJobRequest {
    const message = createBaseCreateBulkJobRequest();
    message.id = object.id ?? undefined;
    if (
      object.operation?.$case === "bulkDeleteWfRun" &&
      object.operation?.value !== undefined &&
      object.operation?.value !== null
    ) {
      message.operation = { $case: "bulkDeleteWfRun", value: BulkDeleteWfRun.fromPartial(object.operation.value) };
    }
    return message;
  },
};

function createBaseGetBulkJobRequest(): GetBulkJobRequest {
  return { id: undefined };
}

export const GetBulkJobRequest = {
  encode(message: GetBulkJobRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      BulkJobId.encode(message.id, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetBulkJobRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBulkJobRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = BulkJobId.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBulkJobRequest {
    return { id: isSet(object.id) ? BulkJobId.fromJSON(object.id) : undefined };
  },

  toJSON(message: GetBulkJobRequest): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = BulkJobId.toJSON(message.id);
    }
    return obj;
  },

  create(base?: DeepPartial<GetBulkJobRequest>): GetBulkJobRequest {
    return GetBulkJobRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBulkJobRequest>): GetBulkJobRequest {
    const message = createBaseGetBulkJobRequest();
    message.id = (object.id !== undefined && object.id !== null) ? BulkJobId.fromPartial(object.id) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
