// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.32.1
// source: wf_run_migration.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { Timestamp } from "./google/protobuf/timestamp";
import { MigrationPlanId, WfSpecId } from "./object_id";

/**
 * metadata object representing actions to take during a wfRun
 * migration
 */
export interface WfRunMigrationPlan {
  /** Name and id */
  id:
    | MigrationPlanId
    | undefined;
  /** When the MigrationPlan was created. */
  createdAt:
    | string
    | undefined;
  /** Name of old thread -> How to migrate thread */
  threadMigration: { [key: string]: ThreadMigrationPlan };
  newWfSpec: WfSpecId | undefined;
}

export interface WfRunMigrationPlan_ThreadMigrationEntry {
  key: string;
  value: ThreadMigrationPlan | undefined;
}

/** How to migrate a thread */
export interface ThreadMigrationPlan {
  /** Name of the new thread we are migrating to */
  newThreadName: string;
  /** name of curNode - > how to migrate curNode to new WfRun */
  nodeMigrations: { [key: string]: NodeMigrationPlan };
  requiredMigrationVars: string[];
}

export interface ThreadMigrationPlan_NodeMigrationsEntry {
  key: string;
  value: NodeMigrationPlan | undefined;
}

/** How to migrate Node */
export interface NodeMigrationPlan {
  /** Currently just migrate to node with this name */
  newNode: string;
}

function createBaseWfRunMigrationPlan(): WfRunMigrationPlan {
  return { id: undefined, createdAt: undefined, threadMigration: {}, newWfSpec: undefined };
}

export const WfRunMigrationPlan = {
  encode(message: WfRunMigrationPlan, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      MigrationPlanId.encode(message.id, writer.uint32(10).fork()).ldelim();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).ldelim();
    }
    Object.entries(message.threadMigration).forEach(([key, value]) => {
      WfRunMigrationPlan_ThreadMigrationEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).ldelim();
    });
    if (message.newWfSpec !== undefined) {
      WfSpecId.encode(message.newWfSpec, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): WfRunMigrationPlan {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWfRunMigrationPlan();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = MigrationPlanId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          const entry3 = WfRunMigrationPlan_ThreadMigrationEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.threadMigration[entry3.key] = entry3.value;
          }
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.newWfSpec = WfSpecId.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WfRunMigrationPlan {
    return {
      id: isSet(object.id) ? MigrationPlanId.fromJSON(object.id) : undefined,
      createdAt: isSet(object.createdAt) ? globalThis.String(object.createdAt) : undefined,
      threadMigration: isObject(object.threadMigration)
        ? Object.entries(object.threadMigration).reduce<{ [key: string]: ThreadMigrationPlan }>((acc, [key, value]) => {
          acc[key] = ThreadMigrationPlan.fromJSON(value);
          return acc;
        }, {})
        : {},
      newWfSpec: isSet(object.newWfSpec) ? WfSpecId.fromJSON(object.newWfSpec) : undefined,
    };
  },

  toJSON(message: WfRunMigrationPlan): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = MigrationPlanId.toJSON(message.id);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt;
    }
    if (message.threadMigration) {
      const entries = Object.entries(message.threadMigration);
      if (entries.length > 0) {
        obj.threadMigration = {};
        entries.forEach(([k, v]) => {
          obj.threadMigration[k] = ThreadMigrationPlan.toJSON(v);
        });
      }
    }
    if (message.newWfSpec !== undefined) {
      obj.newWfSpec = WfSpecId.toJSON(message.newWfSpec);
    }
    return obj;
  },

  create(base?: DeepPartial<WfRunMigrationPlan>): WfRunMigrationPlan {
    return WfRunMigrationPlan.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WfRunMigrationPlan>): WfRunMigrationPlan {
    const message = createBaseWfRunMigrationPlan();
    message.id = (object.id !== undefined && object.id !== null) ? MigrationPlanId.fromPartial(object.id) : undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.threadMigration = Object.entries(object.threadMigration ?? {}).reduce<
      { [key: string]: ThreadMigrationPlan }
    >((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = ThreadMigrationPlan.fromPartial(value);
      }
      return acc;
    }, {});
    message.newWfSpec = (object.newWfSpec !== undefined && object.newWfSpec !== null)
      ? WfSpecId.fromPartial(object.newWfSpec)
      : undefined;
    return message;
  },
};

function createBaseWfRunMigrationPlan_ThreadMigrationEntry(): WfRunMigrationPlan_ThreadMigrationEntry {
  return { key: "", value: undefined };
}

export const WfRunMigrationPlan_ThreadMigrationEntry = {
  encode(message: WfRunMigrationPlan_ThreadMigrationEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      ThreadMigrationPlan.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): WfRunMigrationPlan_ThreadMigrationEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWfRunMigrationPlan_ThreadMigrationEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = ThreadMigrationPlan.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WfRunMigrationPlan_ThreadMigrationEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? ThreadMigrationPlan.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: WfRunMigrationPlan_ThreadMigrationEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = ThreadMigrationPlan.toJSON(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<WfRunMigrationPlan_ThreadMigrationEntry>): WfRunMigrationPlan_ThreadMigrationEntry {
    return WfRunMigrationPlan_ThreadMigrationEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WfRunMigrationPlan_ThreadMigrationEntry>): WfRunMigrationPlan_ThreadMigrationEntry {
    const message = createBaseWfRunMigrationPlan_ThreadMigrationEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? ThreadMigrationPlan.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseThreadMigrationPlan(): ThreadMigrationPlan {
  return { newThreadName: "", nodeMigrations: {}, requiredMigrationVars: [] };
}

export const ThreadMigrationPlan = {
  encode(message: ThreadMigrationPlan, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.newThreadName !== "") {
      writer.uint32(10).string(message.newThreadName);
    }
    Object.entries(message.nodeMigrations).forEach(([key, value]) => {
      ThreadMigrationPlan_NodeMigrationsEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).ldelim();
    });
    for (const v of message.requiredMigrationVars) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ThreadMigrationPlan {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThreadMigrationPlan();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.newThreadName = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          const entry2 = ThreadMigrationPlan_NodeMigrationsEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.nodeMigrations[entry2.key] = entry2.value;
          }
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.requiredMigrationVars.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThreadMigrationPlan {
    return {
      newThreadName: isSet(object.newThreadName) ? globalThis.String(object.newThreadName) : "",
      nodeMigrations: isObject(object.nodeMigrations)
        ? Object.entries(object.nodeMigrations).reduce<{ [key: string]: NodeMigrationPlan }>((acc, [key, value]) => {
          acc[key] = NodeMigrationPlan.fromJSON(value);
          return acc;
        }, {})
        : {},
      requiredMigrationVars: globalThis.Array.isArray(object?.requiredMigrationVars)
        ? object.requiredMigrationVars.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ThreadMigrationPlan): unknown {
    const obj: any = {};
    if (message.newThreadName !== "") {
      obj.newThreadName = message.newThreadName;
    }
    if (message.nodeMigrations) {
      const entries = Object.entries(message.nodeMigrations);
      if (entries.length > 0) {
        obj.nodeMigrations = {};
        entries.forEach(([k, v]) => {
          obj.nodeMigrations[k] = NodeMigrationPlan.toJSON(v);
        });
      }
    }
    if (message.requiredMigrationVars?.length) {
      obj.requiredMigrationVars = message.requiredMigrationVars;
    }
    return obj;
  },

  create(base?: DeepPartial<ThreadMigrationPlan>): ThreadMigrationPlan {
    return ThreadMigrationPlan.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ThreadMigrationPlan>): ThreadMigrationPlan {
    const message = createBaseThreadMigrationPlan();
    message.newThreadName = object.newThreadName ?? "";
    message.nodeMigrations = Object.entries(object.nodeMigrations ?? {}).reduce<{ [key: string]: NodeMigrationPlan }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = NodeMigrationPlan.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.requiredMigrationVars = object.requiredMigrationVars?.map((e) => e) || [];
    return message;
  },
};

function createBaseThreadMigrationPlan_NodeMigrationsEntry(): ThreadMigrationPlan_NodeMigrationsEntry {
  return { key: "", value: undefined };
}

export const ThreadMigrationPlan_NodeMigrationsEntry = {
  encode(message: ThreadMigrationPlan_NodeMigrationsEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      NodeMigrationPlan.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ThreadMigrationPlan_NodeMigrationsEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThreadMigrationPlan_NodeMigrationsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = NodeMigrationPlan.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThreadMigrationPlan_NodeMigrationsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? NodeMigrationPlan.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: ThreadMigrationPlan_NodeMigrationsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = NodeMigrationPlan.toJSON(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<ThreadMigrationPlan_NodeMigrationsEntry>): ThreadMigrationPlan_NodeMigrationsEntry {
    return ThreadMigrationPlan_NodeMigrationsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ThreadMigrationPlan_NodeMigrationsEntry>): ThreadMigrationPlan_NodeMigrationsEntry {
    const message = createBaseThreadMigrationPlan_NodeMigrationsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? NodeMigrationPlan.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseNodeMigrationPlan(): NodeMigrationPlan {
  return { newNode: "" };
}

export const NodeMigrationPlan = {
  encode(message: NodeMigrationPlan, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.newNode !== "") {
      writer.uint32(10).string(message.newNode);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): NodeMigrationPlan {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodeMigrationPlan();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.newNode = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodeMigrationPlan {
    return { newNode: isSet(object.newNode) ? globalThis.String(object.newNode) : "" };
  },

  toJSON(message: NodeMigrationPlan): unknown {
    const obj: any = {};
    if (message.newNode !== "") {
      obj.newNode = message.newNode;
    }
    return obj;
  },

  create(base?: DeepPartial<NodeMigrationPlan>): NodeMigrationPlan {
    return NodeMigrationPlan.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NodeMigrationPlan>): NodeMigrationPlan {
    const message = createBaseNodeMigrationPlan();
    message.newNode = object.newNode ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
